// directory, documents and git follow the source

package br.com.dio;
import br.com.bio.model.Cat;
public class First Program {
    public static void main(String[] args) {
        Book book = new Book();
        Cat cat = new Cat();
    }
}

class Book {
    public String book;
    public integer numPages;
}

// file class in the doc bin 
// docuemnts at file git 
// github, new repository, name test curse dominated ide java 
// description test repository, public, 

package br.com.dio;
import br.com.bio.model.Cat;
public class First Program {
    public static void main(String[] args) {
        Book book = new Book();
        Cat cat = new Cat();

        System.out.printIn(cat);
        System.out.printIn(book);
    }
}

class Book {
    public String book;
    public integer numPages;
}

// doing the version
// right bottom, team, share project, using the create repository
// configyre git repository, select and create the repository dot git 
// git master 
// the symbol show how this tis will do this repository
// the file at left side, right bottom, commit 
// or using the git perspective 
// commit message: first commit 
// commit end push 
// destinatation git repository
URI
//past to the repository path 
// authetication of user and password
// preview 
// push, user and password 
// refresh and up

// use command lines
// tes dominated the IDE JAVA 
// using the command line 

// windows r 
Documents\without-ide
git init 
git add . 
git commit -n  "First commit"
2 files changed, 7 insertions (+)

// doing the push for repository. Copy the line of github. 
// add origin are doing the branch origin
//enter

git push -u origin master 
//enter
1. web browser (default) 2 personal acess token

2
token(password)
// if have some problems...
usename and password 
 2 files:
 firstprogram java 
 firstprogram class

 // debug 


